server.port=8083
spring.output.ansi.enabled=ALWAYS

logging.level.com.damu=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN

management.zipkin.tracing.endpoint=${ZIPKIN_ENDPOINT:http://localhost:9411/api/v2/spans}
management.zipkin.tracing.export.enabled=${ZIPKIN_ENABLED:false}

spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/userdb
spring.datasource.username=damu
spring.datasource.password=password123
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.application.name=USER-SERVICE
spring.config.import=configserver:${CONFIG_SERVER_URL:http://localhost:9296}
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

app.auth.email-verification-url=${EMAIL_VERIFICATION_URL:http://localhost:9090/user/verify-email}
app.auth.password-reset-url=${PASSWORD_RESET_URL:http://localhost:9090/user/reset-password}
app.rabbitmq.exchange=notification.exchange
app.rabbitmq.routing-key=notification.event
app.security.jwt.issuer=${IAM_JWT_ISSUER:http://localhost:8083}
app.security.jwt.audience=${IAM_JWT_AUDIENCE:shop-api}
app.security.jwt.secret=${IAM_JWT_SECRET:2MYI7CSmDvVcNU3IdTyE/rpzUGNkR2cPeabl5+/nuHdukRpVTbiKlwoWRfiZMbKf216DVmqzmnjLyAhgFOstUA==}
app.security.jwt.access-token-ttl-minutes=${IAM_ACCESS_TOKEN_TTL_MINUTES:15}
app.security.jwt.refresh-token-ttl-days=${IAM_REFRESH_TOKEN_TTL_DAYS:30}
